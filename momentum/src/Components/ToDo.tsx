import React, {memo} from 'react';

export const ToDo: React.FC = memo((
    {}
) => {
    // const addTaskCallBack = (e: ChangeEvent<HTMLInputElement>)=>{
    //     addTask(e.currentTarget.value)
    // }
    //create new task
    //change task status
    //delete task
    //? filter tasks
    // const todoListMap = tasks.map((e) => {

    return (
        <>
            <input type={"text"}></input>
            <button>+</button>
        </>
    );
});